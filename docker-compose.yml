services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: readfinder
      POSTGRES_USER: readfinder
      POSTGRES_PASSWORD: readfinder
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
